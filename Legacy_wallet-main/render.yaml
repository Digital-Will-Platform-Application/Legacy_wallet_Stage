services:
  - type: web
    name: legacy-wallet
    env: static
    buildCommand: bun install && bun run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_ENV
        value: production
